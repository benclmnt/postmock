{
  "skipped": [
    {
      "title": "exits 0",
      "reason": "The stream examples share the placeholder stream ID my-broadcasts and run in file order on one postmock, async before sync. This one runs before create_stream.py, so the stream does not exist yet: ErrorCode 1226.",
      "source": "sdk/postmark-python/examples/async/message_streams/archive_stream.py:13"
    }
  ]
}
