{
  "skipped": [
    {
      "title": "exits 0",
      "reason": "The stream examples share the placeholder stream ID my-broadcasts and run in file order on one postmock, async before sync. The sync archive_stream.py archived the stream. Postmark's answer to editing an archived stream is not captured, so postmock answers 501 (docs/04 §2).",
      "source": "sdk/postmark-python/examples/sync/message_streams/edit_stream.py:4"
    }
  ]
}
